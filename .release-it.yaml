git:
  tagName: v${version}
  tagAnnotation: Release v${version}
  commitMessage: "chore: release v${version}"
  requireBranch: main
github:
  release: true
  releaseName: "v${version} "
npm:
  publish: false
hooks:
  after:release: task major-tag TAG=v${version}
